# HTTP 블록 내에 server 블록을 포함
http {
    server {
        listen 80;
        server_name _;

        location / {
            root /usr/share/nginx/html;
            try_files $uri /index.html;  # SPA 라우팅 처리
        }
    }
}
